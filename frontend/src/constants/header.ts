import { PATHS } from "./url";

export const HEADER_PAGE_TABS = {
    HOME: {
        id: "home",
        label: "Home",
        path: PATHS.HOME,
    },
    RULES: {
        id: "rules",
        label: "Rules",
        path: PATHS.RULES,
    }
}